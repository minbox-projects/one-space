# Project memory

OneSpace 是面向开发者的 macOS 桌面工作台（Tauri 2 + React 19 + TypeScript），把 AI CLI 环境、原生终端会话、MCP、Skills/Subagents、工作流与常用生产力工具收拢到一个窗口。本文件是原生 agent 共享的架构与约定基线；`.ai-workflow/index/navigation.json` 是权威导航索引，`navigation.md` 由其生成。

## 技术栈与构建

- 前端：React 19、TypeScript、Vite 7、Tailwind CSS 3、Radix UI、i18next（中/英）。
- 后端：Rust、Tauri 2（`src-tauri/`），插件含 dialog / shell / process / updater / global-shortcut。
- 前端命令封装集中在 `src/lib/*`，通过 `@tauri-apps/api` 的 `invoke` 调用后端；UI 组件不直接拼装命令字符串。
- 构建命令：`npm run dev`、`npm run build`（`tsc -b && vite build`）、`npm run tauri dev`、`npm run tauri build`。
- 检查命令：`npm run lint`（eslint）、`npm test`（vitest run）、`npm run check:cli-matrix`；后端测试在 `src-tauri/` 下用 `cargo test`。
- 后端测试构建采用 `[profile.test]`：`opt-level = 1`、`debug = 0`、`split-debuginfo = "off"`，`[profile.test.package.sha2] opt-level = 2`；优先改善运行时间，不承诺增量重编提速。保持生产密码学参数（PBKDF2 100,000 次迭代，对应 `src-tauri/src/crypto.rs` 的 `PBKDF2_ITERATIONS`）、`[lib] crate-type` 和 vendored OpenSSL / bundled SQLite 不变；macOS 系统库候选暖重编收益 0.80%，未达 15% 采用门槛。
- 后端测试隔离：仅依赖 `get_app_dir()` 的测试可使用 `cfg(test)` 线程局部应用目录 guard；直接修改 HOME 或使用全局 server 状态的测试保留串行，不能假定线程局部覆盖会跨线程传播。内容哈希与 mtime 无关的测试用 `File::set_modified` 显式改变 mtime；重试/退避测试使用 paused 时间与到达间隔断言，真实 socket header / OS connect 行为保留真实时间。
- 测试计时分别记录 harness 与原始 wall 时间，争用扣除估算不能当作实测 wall；stable 不支持 `--report-time` 时使用已授权的等价计时。2026-09-18 交付快照：本次实现已合并至 main `29b31c0`；root 最终 `cargo test --manifest-path src-tauri/Cargo.toml` exit 0，lib 609 通过、两个既有 ignored，harness 29.57s，原始 wall 107.18s 包含切换 root 后重编，不是热运行 wall；bin/doc 各 0 测试且成功，前端代表测试 2 文件共 55 通过，集成 check 通过。变更独立基线阶段的 581 例（579 通过、两个既有 ignored）及 harness 中位数 27.68s 保留为历史数据；该阶段新增一例并发隔离回归、按用户后续明确范围修订删除一例冗余 E2E，删除不作为原指标达标手段；同期 main 新增 30 例，最终快照共 611 例含两个既有 ignored，数量不是永久标准。原 AC-003 单例 <100ms（实测 0.18–0.37s）、AC-004 增量改善 ≥10%（5.94→6.54s，慢 0.60s）仍未达标，用户已明确同意保留两项偏差；一次双轴审查已完成。完整授权、命令级实测与保留覆盖见 [Cargo test speedup](.ai-workflow/notes/implemented/testing/2026-09-18-cargo-test-speedup.md)。
- 版本号三处保持一致：`package.json`、`src-tauri/tauri.conf.json`、`src-tauri/Cargo.toml`。

## 模块根与职责

导航索引登记四个模块根，feature 的 `owner_role` 必须与所属模块根一致：

- `src`（owner `frontend`）：React 界面、状态、Tauri 命令封装与 i18n。
- `src-tauri/src`（owner `backend`）：Rust 命令、存储引擎、运行时与外部集成。
- `docs`（owner `documentation-maintainer`）：用户手册、CLI/MCP/Skills 文档与设计报告。
- `tools`（owner `backend`）：本地校验脚本。

## 前端架构（`src/`）

- `src/App.tsx` 是外壳与总控：侧边栏、页面切换、全局状态与快捷键；`src/lib/navigation.ts` 负责旧标签到新导航目标的解析（`resolveNavigationTarget`）。
- 每个业务域是一个 `src/components/<Domain>/` 目录或同名组件；每个组件目录通常包含 `index.tsx`、子组件、`*.test.tsx`，复杂域再拆分 `components/`、`hooks/`、`helpers/`、`types.ts`（参见 `Workspaces/`）。
- 命令封装与领域类型放在 `src/lib/`，按域一文件（如 `workflows.ts`、`skills.ts`、`subagents.ts`、`sshTunnels.ts`、`fileSharing.ts`、`shortLink.ts`、`aiAssistant.ts`、`apiGateway.ts`）。
- 文案统一走 `src/i18n.ts`，新增界面文本必须同时提供中英文；`en_keys.txt` / `zh_keys.txt` 为键清单。
- 共享基础组件在 `src/components/ui/`，Provider（主题、Toast、确认框、错误边界）在 `src/components/` 顶层。

## 后端架构（`src-tauri/src/`）

- `lib.rs` 声明模块并由 `app_runtime::run` 启动；`app_runtime/` 负责窗口、托盘、全局快捷键、CLI 入口与 OAuth。
- 每个业务域一个根文件加同名子目录（如 `ai_sessions.rs` + `ai_sessions/`、`skills.rs` + `skills/`、`protocol_router.rs` + `protocol_router/`）。子目录按 `commands`、`types`、`runtime`、`tests` 等拆分。
- `app_store/` 是统一存储与迁移核心：`storage_engine.rs`、`migration.rs`、`provider_projection/`、`sync.rs`、`types/`；会话、provider 与 launcher 命令都在此汇聚。
- 配置与密钥：`config.rs`、`runtime_profiles.rs`、`claude_profiles.rs`、`secrets.rs`、`crypto.rs`。
- CLI 探测与版本：`cli_probe.rs`、`cli_updates.rs`、`version_detect.rs`。
- OpenCode CLI 探测按完整 semver 优先级解析并比较版本，兼容 1.x 与 2.x：prerelease 的纯数字标识低于字母数字标识、stable 高于 prerelease，build metadata 保留在显示文本中但不参与优先级比较；检测到多个安装时选择最高版本，更新检查同样容忍 build metadata。
- OpenCode 会话历史与用量同时读取 legacy JSON、SQLite v1（`session` / `message`）和 SQLite v2（`session_v2` / `session_message`）。trim 后相同的 session ID 固定按 v2 > v1 > JSON 选择来源，低优先级来源只补充独有 session；用量在选中来源内保留该 session 的全部 message，以 message 级 token 数据兼容早期 v1 并匹配 v2，跨来源不重复累计。Tauri schema、前端与 resolver 保持不变；原因与取舍见 [OpenCode session storage compatibility](.ai-workflow/notes/implemented/bug-fix/2026-09-20-opencode-session-storage-compatibility.md)。
- Antigravity 用量三来源：legacy `~/.gemini/tmp` 下 `session-` 前缀 `.json`/`.jsonl` 按迁移前 gemini 口径解析 token（`.json` 的 `cached+cache` 求和、`message.metadata.model` 回退）并产出 `UsageRecord`；新格式 brain transcript（两处 brain 根下 `transcript_full.jsonl`，`USER_INPUT` 大小写不敏感）只计 `scanned_sessions`/`scanned_calls`，不进 token 聚合；额度经 `sessions_antigravity_quota` 直读 `agy -p /usage --output-format json`（成功结果 5 分钟 TTL、失败不缓存），不走扫描缓存。`empty` 契约：`available` 且 `scanned_sessions==0` 即 `empty`，有源无 token 时前端显示 Antigravity 专属 token 不可用说明。原因与取舍见 [Antigravity Usage and Quota](.ai-workflow/notes/implemented/architecture/2026-09-21-antigravity-usage-and-quota.md)。

## Skills 统一目录与兼容

- `~/.agents/skills` 是所有 Skills 安装、扫描、同步与显示的规范目录；迁移后不再按工具维护独立 Skills 目录。
- `~/.claude/skills` 是指向 `~/.agents/skills` 的兼容符号链接；若该路径被普通文件/目录或错误、损坏的符号链接占用，则保持原样并返回可操作的失败，绝不覆盖。
- 同名冲突以统一目录版本为准；工具特定版本备份到 `~/.agents/skills/.backups/<tool>/<skill>/<content-hash>/`，按来源工具、Skill 名与内容哈希做幂等键，重复初始化不产生重复备份。
- `.backups`、`.stage-*`、`.backup-*` 是内部产物，绝不视为 Skill：扫描、迁移、投影、哈希、复制与计数全部跳过，避免递归膨胀。启动初始化时清理统一与各工具 skills 根下的 `.stage-*`/`.backup-*`、删除工具根 `.backups` 副本；统一 `.backups` 出现递归嵌套污染时整体删除，未污染的合法备份保留。
- 兼容性矩阵由后端记录 Claude / OpenCode / Codex / Antigravity 对 `~/.agents/skills` 的读取行为；无法直接读取的工具显式标记为依赖兼容路径或不受支持。

## API 网关模块与边界

- API 网关是独立模块：前端域 `src/components/ApiGateway/` 加命令封装 `src/lib/apiGateway.ts`，后端 `src-tauri/src/api_gateway.rs` 加同名子目录（`types_config`、`storage`、`selection`、`runtime_http`、`forwarding`、`commands`、`usage_log`、`templates`），模板快照为同目录内置 `provider_templates.json`。
- 导航 id 固定为 `api-gateway`（feature 归属模块根 `frontend`，owner `frontend`）与 `api-gateway-backend`（模块根 `tauri-backend`，owner `backend`）。`api-gateway` 是左侧「AI 能力」分组的顶层页签，位于 `ai-environments` 与 `ai-usage` 之间；`resolveNavigationTarget("api-gateway")` 解析为顶层 tab。API 网关不作为工具存在：已从 `MoreToolsSection`、`moreToolPresentation`、`launcherToolVisibility`、`MoreToolsHub.tsx` 卡片与 `Launcher.tsx` 内部工具清单移除，仅保留在 `Launcher` 的导航目标中。新增工具 id 必须同时接入 `navigation.ts`、`moreToolPresentation.ts`、`launcherToolVisibility.ts`、`MoreToolsHub.tsx`、`Launcher.tsx` 与 `App.tsx`，否则页签不可达或启动器清单不一致。
- 上游服务商保留 `protocol` 字段（`chat_completions` 默认 / `responses`）作为继承来源；上游服务商配置支持 `weight: u32`（默认 1，范围 1 到 100（`MIN_PROVIDER_WEIGHT` 到 `MAX_PROVIDER_WEIGHT`），`#[serde(default = "default_provider_weight")]`，旧 `api_gateway.json` 反序列化缺省为 1；保存时经 `api_gateway_save_config` 与 `api_gateway_upsert_provider` 严格校验，超出范围拒绝写入并返回可操作错误）；前端在 `ProviderDetailDialog` 中提供 1–100 权重输入与范围校验，服务商卡片展示 `Weight: X` / `权重: X` 徽标；每条模型映射可另行声明 `protocol`，缺省或 `null` 即继承所属服务商协议；映射还可选填 `display_name`（`Option<String>`，缺省省略序列化字段），作为网关为该本地模型展示的名称，未填写时回退到远端模型名；映射新增 `reasoning_efforts: Vec<String>`（serde 默认、空时省略序列化）保存该模型的推理强度档位，仅在服务商详情手动维护，服务商模板与模板同步不携带、不写入该字段；每条映射新增持久化 `enabled`（`#[serde(default = "default_true")]`，字段缺省（不出现）视为启用、始终序列化，旧 `api_gateway.json` 无需迁移），另含五个 serde 默认的运行时健康字段 `auto_disabled`、`disabled_reason`、`disabled_at`、`consecutive_failures`、`last_error_at`；禁用或自动禁用映射不参与候选、不成为命中、其 `upstream_model` 绝不参与转发，请求只命中某服务商的禁用或自动禁用映射时该服务商不得回退 `default_model`（等价于未命中），完全未命中任何映射仍按既有规则在协议一致时回退 `default_model`；映射级 `enabled`（用户意图）与映射级 `auto_disabled`（运行状态）相互独立：手动禁用/启用服务商（`set_user_enabled`）绝不修改任何映射的状态，被用户禁用的映射只能由对该映射的显式启用恢复；服务商级 `auto_disabled` 等运行字段为遗留字段，读取配置时由 `normalize_config` 一律清空；自动禁用的恢复命令为 `api_gateway_reenable_provider_model`（按服务商与 trim 后 `(local_model, upstream_model)` 键清除匹配行的运行状态，未知键或空键返回可操作错误且不写盘）与 `api_gateway_reenable_provider_models`（清除某服务商遗留的服务商级运行字段与所有自动禁用行的运行状态，`enabled=false` 的用户禁用行保持禁用且其计数保留），`api_gateway_reenable_provider` 已移除；`api_gateway_upsert_provider` 保存时按同一键保留既有行的运行状态，新键或改动键的行以健康状态开始。终端同步写入的模型清单只取服务商 `enabled` 且映射 `enabled && !auto_disabled` 的行，`opencode` 以映射的 `local_model` 为键写入 `tool_config.models`（值中的名称取 `display_name`，缺省回退远端模型名；映射 `reasoning_efforts` 非空时另写 `reasoning: true` 与 `variants`（按档位标识为键、值为 `{"reasoningEffort": <档位>}`，空白档位丢弃、重复合并），为空时保持只写 `name`），`codex` 的 `model` 取首个非空启用映射 `local_model`，没有任何启用映射时才回退首个非空 `default_model`，仍为空则省略该键。候选选择依据映射行协议（缺省继承服务商协议）能否服务入站协议，不再用服务商 `protocol` 字段硬过滤；命中映射但协议不一致的服务商不参与本次请求且不回退 `default_model`，未命中映射才回退 `default_model` 并要求服务商协议一致。`/v1/models` 与前端聚合视图（`aggregateModels` 与「模型列表」页签）同样只包含服务商 `enabled` 且映射 `enabled && !auto_disabled` 的本地模型；前端 `ProviderDetailDialog` 为每个映射行提供启用开关，用户禁用行以 `data-disabled="true"` 与弱化样式直接可见，自动禁用行以 `data-auto-disabled="true"` 与琥珀色样式标记、并提供按映射键的「重新启用映射」按钮，服务商卡片在存在自动禁用映射时显示只读的自动禁用映射计数提示，`ProviderDetailDialog` 在服务商存在任一自动禁用行时提供「重新启用所有映射」按钮，`resolveMappingPreview` 在只命中禁用或自动禁用映射时返回 `null`。「模型列表」页签紧随「上游服务商」并以 `aggregateModels` 的同一行集（服务商 `enabled` 且映射 `enabled && !auto_disabled` 的本地模型，不再包含默认模型条目）为唯一来源，一个本地模型一行、保持原有的三列（模型 ID、模型名称、上游来源）；第二列（模型名称列）在模型名称与调用协议徽标下方，展示该模型聚合支持的推理强度档位列表（`reasoningEfforts`，由 `resolveAggregatedReasoningEfforts` 从所有启用上游映射中去重合并，未配置时不占额外空间，不新增独立表格列）；模型名称由 `resolveAggregatedModelName` 按聚合顺序解析，取首个 trim 后非空的映射 `display_name`，否则取首个映射来源的 `upstream_model`；上游列按聚合顺序显示「服务商名 + 上游模型 ID」；页签徽标数与聚合模型指标卡同源（均为 `aggregateModels(config.providers).length`），搜索框仅按模型 ID 与解析后名称做 trim 后大小写不敏感子串匹配（空或全空白显示全部），绝不匹配上游服务商名或上游模型 ID；面板常驻挂载并以 `hidden` 切换以保留查询，聚合为空显示空态、非空查询无匹配显示无匹配态。中继不做请求体转换，调用方协议需与服务商或映射行协议一致；`/v1/messages`（Anthropic）不受支持属已知边界。
- 「服务商模板」是携带可选模型清单 URL 的端点定义（中文「服务商模板」，英文概念 `Provider Template`、区块标题 `Provider Templates`；既有「上游服务商 / Upstream providers」称谓保持不变）：内置快照 `src-tauri/src/api_gateway/provider_templates.json` 由 `templates.rs` 以 `include_str!` 解析为 `ProviderTemplate` / `ProviderTemplateModel`，两个内置模板 `opencode-zen` 与 `commandcode` 携带 `base_url` 与预配置的 `models_url`（`https://opencode.ai/zen/v1/models`、`https://api.commandcode.ai/provider/v1/models`，`source` 同 URL）且 `models` 为空；`ProviderTemplate` 支持可选 `icon` 字段（内置快照分别配置 `opencode` 与 `commandcode`，提供 OpenCode/CommandCode/OpenAI 等官方图标支持并在新增服务商、模板弹框与绑定模板的上游服务商卡片（按 `template_id` 展示模板头像）展示，统一采用与 OneSpace `ServiceProviderAvatar` 规范完全一致的微渐变、精细边框与柔和阴影的立体头像底座与居中比例），`ProviderTemplateModel` 仅含 `upstream_model`、可选 `display_name`、可选 `protocol` 与持久化 `enabled`（缺省（不出现）视为启用、始终序列化），`ProviderTemplate` 无 `snapshot_version`，模板不携带任何价格、峰谷或推理强度数据。`api_gateway_provider_templates` 按快照顺序返回内置模板并追加已持久化的自定义模板视图（含 `synced_at`、`source`、`from_snapshot`），已持久化同步结果优先于内置快照。用户手动触发 `api_gateway_sync_provider_template` 时模板必须配置非空 `models_url`（为空即返回可操作错误且不写盘），后端以 `reqwest` GET 该 URL（15 秒超时、不带凭据）并整体替换模板模型清单：接受 `data` / `models` / 根数组 / 纯字符串条目，对象条目 `id` 为上游模型名、`name` 经补全后作为覆盖整个标识的显示名（源名为基、缺名或空白时回退同一标识的已存本地名、两者皆无时取标识段变换），`supported_endpoints` 为数组时 `/chat/completions` 优先于 `/responses`、两者都不含则丢弃该条目，字段缺失或非数组则保留条目且协议为空并继承模板协议；同名模型由源提供的 `protocol` 覆盖、显示名取补全后的名称，源未提供则保留本地值，`enabled` 永远保留本地值，新模型默认启用，重复标识保留首个；网络失败、超时、非 2xx、非 JSON、缺模型数组、条目缺标识与空有效集为致命错误（报 URL 与原因）且绝不写盘。`GatewayConfig.provider_templates: Vec<ProviderTemplateState>` 持久化模板状态（含模板、`synced_at`、`source`），全部 serde 默认，旧 `api_gateway.json` 免迁移可读。
- 模板绑定与维护：`GatewayUpstreamProvider` 携带 `template_id: Option<String>` 与 `ignored_models: Vec<String>`，同步只增量下发、绝不整体替换派生服务商：仅当字段仍等于上一版模板值时才更新（服务商 `name`/`base_url`/`protocol`，映射 `display_name`/有效协议，其中映射有效协议等于所属服务商协议时存 `None`（跟随服务商协议）否则存生效协议），映射 `local_model` 绝不被改动，既有映射的 `enabled` 绝不被重新启用：同步把上一版模板已移除的模型对应派生映射禁用（保留行、不删除、不写价格行/`ignored_models`/新字段），模型回归模板也不自动重新启用；只有模板中启用且不在 `ignored_models` 的模型才补齐启用映射（同样有效协议等于服务商协议时存 `None`），模板中禁用的模型绝不传播，模板不再携带的映射保留在服务商并由 `isMappingDeprecated` 标记 deprecated、不删除，被同步禁用后不再参与转发与聚合；由于模板与同步都已无价格数据，同步绝不创建或修改价格行；删除的模型记入 `ignored_models`（仅一次）以防后续同步复活，手动创建的（无 `template_id`）服务商完全不受同步影响；整次同步在配置克隆上暂存并只经一次加密 `write_config` 原子落盘，失败则配置保持原状；写入边界仅为模板状态与派生服务商的字段/映射，绝不触碰本地 Key、`terminal_syncs`、转发运行状态、价格行或历史用量记录。`api_gateway_create_provider_from_template(template_id, name, base_url, protocol, api_key)` 拒绝空 Key 且不写入，名称/`base_url` 为空回退模板值、协议以参数为准，新服务商携带 `template_id`、`default_model` 为空，并只为模板中启用的模型生成启用映射（`local_model` 为模板去空白后的 `local_model`、缺失时回退 `upstream_model`，`display_name` 原样携带模板值、缺失或空白时按标识段变换补全，生效协议等于新服务商协议时存 `None`（跟随服务商协议）否则存生效协议），不写任何价格行；`api_gateway_delete_provider_model` 在绑定与手动服务商都删除映射与该模型专属价格行，并在绑定服务商上恰好一次记录忽略模型，`api_gateway_restore_provider_model` 只恢复被忽略模型并仅重建映射（同样生效协议等于服务商协议时存 `None`，不写价格行），模板已无该模型时返回可操作错误且不写入。
- 中继接受 `/chat/completions`、`/responses` 及其无 `/v1` 形式并统一成 `/v1/...` 上游路径，拼接上游 URL 时折叠重复的 `/v1` 版本段，因此 `base_url` 带不带 `/v1` 均可。转发按拒绝列表透传客户端入站请求头（如 `x-opencode-session`），hop-by-hop 与传输头以及 `content-type`、`accept` 丢弃；本地中继凭据（本地 Key 的 `authorization` / `x-api-key`）一律不得透传给上游，上游凭据由中继显式设置为 `authorization: Bearer <provider api_key>`。
- 新建本地 Key 只需名称，值由后端用 OS 熵随机生成（`sk-gateway-<128bit hex>`）；编辑既有 Key 时留空或回传脱敏占位符保留原值。
- 本地服务固定监听 `127.0.0.1` 加配置端口（默认 `17688`），bind 失败即返回包含端口与原因的可操作错误，不回退到其他端口；启用状态持久化，重启后按上次状态自动恢复监听。对外展示、复制与终端同步使用的本地 Api 地址统一为带 `/v1` 后缀的 `http://127.0.0.1:<端口>/v1`（`GatewayStatus.local_base_url` 与前端 `localBaseUrl` 同源），中继本身仍同时兼容无 `/v1` 的入站路径。
- 上游服务商、本地 Key 与终端同步台账保存在独立加密文件 `api_gateway.json`（经 `crate::crypto` 加密并临时文件加 rename 原子写入），与 Protocol Router、AI Environments 的存储互不共享，密钥不得以明文落盘。
- 请求日志与用量另存于 `get_app_dir()` 下独立、不加密的 SQLite 文件 `api_gateway_usage.db`（`api_gateway/usage_log.rs`，`UsageLogStore`，表 `usage_logs`）；存储格式兼容结果 `success|failure|cancelled`，但当前业务请求日志只新写正常完成的 success/failure 结果，`cancelled` 仅为历史数据兼容值。记录字段包括 UTC+8 时间戳、本地/上游模型名、服务商 id 与名称、HTTP 状态、输入/缓存读/缓存写/输出四档 token、总 token、金额、耗时、可空 `error_message` 与非空 `terminal`（缺省即终止行），绝不包含请求/响应正文、请求头或任何凭据，也绝不保存完整原始响应体或原始 HTML。仅通过本地鉴权并进入 `/chat/completions` 或 `/responses` 且正常完成业务结果的请求会写入；本地 401、`GET /v1/models` 与未知路径/方法不记录。非流式从缓冲响应体解析 usage，流式由 `SseUsageAccumulator` 只读累积且不改写转发字节，同时接受 Chat Completions 事件顶层 `usage` 与 Responses 的 `response.completed.response.usage` 并保留最后一个有效对象；网关绝不注入 `stream_options.include_usage`。每个新解析的 usage 归一化为互斥四档 `input_tokens`（普通输入）/`cache_read_tokens`/`cache_write_tokens`/`output_tokens`：OpenAI Chat/Responses 以 `input_tokens` 优先于 `prompt_tokens` 的 reported 总量经 checked subtraction 减去嵌套 `cached_tokens`（可选嵌套 `cache_write_tokens`）得到普通输入，混合兼容形态用顶层 `cache_creation_input_tokens` 作为缓存写回退且嵌套缓存写优先；Anthropic 风格无嵌套明细时顶层 input 已是普通输入、`cache_read_input_tokens` 与 `cache_creation_input_tokens` 为独立档位；嵌套缓存读与顶层 `cache_read_input_tokens` 冲突、或嵌套分量超过 reported input 属非法形态，标记无效且绝不用饱和减法修复，只采用不重复计价、非负的保守输入回退并保持该请求缓存统计不合格，非法缓存档位不参与缓存分档计费。缺失 usage 记 `usage_present = false` 与零档位，无 usage 仍记录，只有写入失败被记录并吞掉，绝不影响响应；历史 token 与金额永不重算。旧库由 `PRAGMA table_info` 幂等追加 `usage_semantics`（既有行默认 `legacy`、新行 `canonical_v1`）、`usage_present` 与 `cache_accounting_valid`（连同既有的终止、错误文本与推理档位列，重开不重复变更），绝不改写既有 token、total、amount 与日志字段，legacy 行被排除在新缓存分子/分母之外；请求运行时经 `UsageLogStore::append_with_accounting`（单行）与 `append_batch_with_accounting`（一个连接内按序写入整批行）写入每行真实的 `usage_semantics`/`usage_present`/`cache_accounting_valid` 并执行同一保留清理，`append` 与 `append_batch` 是仅测试用的规范契约便捷方法（`#[cfg(test)]`）。正常完成的 success、failure、重试恢复与无候选请求保持「每个已完成的上游尝试一行 + 恰好一个终止行」：失败尝试作为非终止行绑定该次服务商与上游模型，成功、`ReturnToClient`、中途流失败或全部耗尽时按时间最后完成的尝试成为终止行，无候选沿用既有合成 502 行。下游 TCP 关闭或响应不可交付属于工具 transport 生命周期而非业务请求日志结果：该 inbound request 的整个缓冲日志集合一并丢弃，包括断开前已经完成的 attempts，不写合成 `cancelled` 终止行。失败尝试的 `error_message` 只存上游可读错误信息：合法 JSON 信封的 `error.message`，否则为 lossy 解码、去 HTML 标记、折叠空白并 trim 的可读摘要，传输/流失败存其描述，未到达上游或清洗后为空存 `null`；写入前把该服务商 API Key 原样替换为 `[redacted]`、掩蔽 `Bearer`/`sk-` 形状凭据并截断到 4096 字符（Unicode 标量边界，仅截断时追加 `…`）。
- 金额在写入记录时只按本次实际转发的服务商自身的价格行与上游模型名精确匹配（`compute_cost_at_time` / `match_price_for_provider`，服务商 id 与上游模型名两者都精确匹配且大小写敏感，其他服务商的行与 `provider_id` 缺省的全局行绝不匹配，不再有任何全局或跨服务商回退；输入/缓存读/缓存写/输出四档单价，美元/百万 tokens，支持可选配置 UTC+8 峰谷时段 `off_peaks` / `off_peak`，每个时段可用可选 `days`（`0` 周日–`6` 周六、UTC+8；缺省/空表示每天，读写均归一化为去重升序并丢弃越界值）限定生效星期，多时段按列表顺序首个「星期与时刻」都命中的时段生效并兼容单时段，无峰谷时段按标准价格计费）并固化进该行，之后修改价格永不重算历史；未定价模型存 `None`、界面显示 `—`、不计入合计，界面另显示未定价请求数。读取配置时对价格表做幂等归一化：全局价格行按各服务商可达的映射上游模型（非空映射 `upstream_model` 加去空白后的 `default_model`）迁移为每个可达且尚无该模型价格行的服务商的专属价格行，无法匹配的全局行、服务商不存在或模型不可达的行一律删除（手动服务商同样适用），按 `(provider_id, upstream_model)` 去重保留首个，归一化只改内存并由下一次写入持久化，绝不改动服务商、Key、终端同步与历史用量。价格维护入口位于「上游服务商」页签的新增/编辑服务商对话框：每条模型映射行内嵌 `MappingPriceEditor`，编辑器默认随映射行的展开箭头（无障碍名称「映射详情 / Mapping details」）收起，展开后与推理档位一同显示，维护输入/缓存读/缓存写/输出四档单价（美元/百万 tokens）与可选 UTC+8 峰谷时段（可多段、支持星期多选或每天），同一上游模型的多行共享一份草稿；四档全空表示未定价、不产生价格行，任一档有值即定价（空档按 0，显式 0 计为 0 成本）；保存服务商时通过 `api_gateway_upsert_provider` 一次调用把服务商与其完整价格行集合原子保存，删除映射或服务商时对应价格行同步移除，「用量统计」页签不再提供价格入口。默认模型同样在该对话框内维护，改为从该服务商已映射上游模型中选择的下拉（含空选项）；打开时若已保存的 `default_model` 未被任何映射覆盖，则物化为一条标记为自动新增的映射行（`local_model = upstream_model`、启用、协议继承）并携带该服务商已有的同模型价格行，选择与转发目标保持不变，保存即持久化该映射与价格。
- `GatewayConfig` 新增 `usage_retention_days`（默认 90，范围 1–365，`#[serde(default)]`）与 `model_prices`（`#[serde(default)]`），旧 `api_gateway.json` 无需迁移仍可反序列化；价格随 `api_gateway_upsert_provider` 调用原子保存（可选 `prices` 列表缺省 `None` 表示保持该服务商价格行不变），`api_gateway_delete_provider` 同时删除该服务商的价格行，独立的 `api_gateway_model_prices_get` 与 `api_gateway_model_prices_save` 命令已移除；保留天数保存先读取现有配置、只替换目标字段再写回，绝不清空服务商、本地 Key 或 `terminal_syncs`，非法保留天数返回可操作错误且不落盘。保留天数在设置页独立分区 `ai-gateway` 配置，每次写入新日志时永久删除超期记录。四个命令 `api_gateway_usage_stats`、`api_gateway_request_logs`、`api_gateway_usage_retention_get`、`api_gateway_usage_retention_save` 提供查询与配置，聚合、分组、筛选与分页（每页固定 50）均在后端按 UTC+8 完成。`api_gateway_request_logs` 的不分组规范请求值为 `group_by: "none"`（前端即如此发送），`null`/缺省/空串兼容为不分组别名，分组值为 `"model"` 与 `"day"`，其他值返回可操作错误；不分组响应的 `group_by` 为 `null`，并返回覆盖已解析范围、独立于分页与模型筛选的去重非空 `local_model` 有界 `models` 列表以驱动前端模型筛选；tokens 与花费来自成功响应以及记录了已累积用量的失败（流式尝试在首个转发字节之后失败时记状态 502 的 `failure` 但保留已累积 usage；非流式只从 2xx 响应体解析 usage，未捕获用量的失败四档 token 全为 0），`unpriced_count`（与 `—` 显示）只统计已到达上游模型、没有匹配价格行且会产生用量成本的请求（success 或 `total_tokens > 0`），零用量失败与无上游失败按 0 成本且不计入未定价；请求数仍统计全部终止行，tokens 与花费只累加各行实际记录的用量。用量统计（totals、时间分桶、按模型、按服务商明细、`unpriced_count`）只聚合终止行，服务商明细另排除 `provider_id=''`（无候选 failure 仍计入 totals/models），存在未定价请求时同步聚合返回 `unpriced_items`（含模型服务商、模型 ID 及请求数）供界面展示提示。totals、时间分桶、按模型与按服务商明细的 `UsageMetrics` 都始终序列化五个附加字段 `cache_hit_tokens`、`cache_eligible_tokens`、`cache_hit_rate_percent`（`0..=100` 百分数或 `null`）、`cache_rate_eligible_count` 与 `successful_request_count`，命令名 `api_gateway_usage_stats` 与既有字段不变；分子为合格行 `cache_read_tokens` 求和、分母为合格行 `input_tokens + cache_read_tokens + cache_write_tokens` 求和、分母为 0 时返回 `null`，所有层级共用同一合格谓词（终止、`result='success'`、`usage_semantics='canonical_v1'`、`usage_present=1`、`cache_accounting_valid=1` 且分母大于 0），同一本地模型跨服务商先分别求和分子/分母再计算一次 token 加权百分比（绝不平均百分数），服务商明细仍按 `local_model + provider_id + provider_name` 分离；`successful_request_count` 统计范围内成功终止行，`cache_rate_eligible_count` 只统计合格行，legacy/无 usage/零分母/非法成功计入成功但不合格，失败、取消与无候选两者都不计。前端用量面板直接渲染后端 `cache_hit_rate_percent`（`null` 显示 `—`、真实 `0.0` 显示 `0%`）而不再自行推算，覆盖显示为合格请求数/成功请求数，指标名固定为中文「Token 缓存命中率」、英文「Token Cache Hit Rate」；原因与取舍见 [API Gateway Cache Hit Rate Normalizes Provider Usage Semantics](.ai-workflow/notes/implemented/bug-fix/2026-09-21-api-gateway-cache-hit-accounting.md)。历史 `result='cancelled'` 行不删除、不迁移，Rust parser/enum、TypeScript type/translation helper 与兼容 status filter 输入继续保留，但所有用户可见请求日志查询统一排除这些行，包括 records、`total`、`total_pages`、页码收敛、`models` 面、按模型/按天分组的请求数与错误数以及最后请求时间；兼容 `status="cancelled"` 返回合法空页。前端状态筛选只提供 success/failure，并防御性丢弃 stale compatibility payload 中的 cancelled 记录；正常 success/failure/recovered/no-candidate 的尝试行、终止行、状态、tokens、费用、错误文本与标签语义不变。
- 终端写入边界：仅在用户主动「添加服务商 / 同步」时写入。同步不改写既有 `opencode`/`codex` 服务商记录，而是为每个受支持工具（`opencode`、`codex`）创建或更新一条名为 `API Gateway` 的独立网关服务商记录，携带本地 Api 地址、解析后的默认本地 Key 值（存储位指向禁用 Key 或缺省时按列表顺序兜底，与前端 `resolveDefaultKeyId` 一致）与网关的模型映射列表，并写入 `api_gateway_gateway` 标记（顶层或 `tool_config` 内均可）；`opencode` 网关记录统一使用 `provider_key = "apigateway"`。同步成功后自动激活 `opencode` 下的网关服务商并投影写入 `~/.config/opencode/opencode.json`（含 `options.apiKey`），使网关条目与 Key 真正落盘（`codex` 保持手动激活与手动投影）。再次同步优先复用 `terminal_syncs` 台账的 provider id，但仅当该 id 指向同一工具且带网关标记的服务商时才复用；指向未标记的用户记录视为过期台账，改为新建带全新 UUID 的独立网关记录，绝不改写用户记录；台账不可用时再取该工具已带标记的网关记录，都没有才新建 id。`api_gateway_terminal_targets` 对每个受支持工具各返回一个目标并套用同一标记规则（未标记的服务商不算已同步），`api_gateway_configure_terminal` / `api_gateway_sync_terminal` 以 `target_tools`（前端封装键 `targetTools`）传入工具名；工具名大小写不敏感校验，统一以小写 `opencode` / `codex` 存储。不改写 Protocol Router 的 route 数据，不触碰 `claude`/`antigravity` 记录。终端同步按钮按工具隔离进行中状态：仅当前操作的按钮禁用并转圈，其他工具按钮仍可点击；终端目标行展示本地 Base URL 及基于 `synced_at` 的最近同步时间（未同步展示“尚未同步”）。
- 失败分类集中在 `selection::classify_failure`（无消息上下文时 429 恒为 `Transient`）与 `selection::classify_failure_with_message`（有上游错误文本时判定额度耗尽：`selection::is_quota_exceeded_message` 命中 `quota` / `billing` / `insufficient` / `usage limit` / 周月期 / `upgrade plan` / 余额或计划组合信号时 429 升级为 `Retryable`，裸 `limit` / `exceeded` 如 `Rate limit exceeded` 仍为临时限流 `Transient`），HTTP 状态语义优先于错误响应体是否为 JSON：401/403 立即禁用并切换；404 仅跳过该服务商，本请求不再尝试它，继续其他候选；408/5xx/额度耗尽 429 计健康失败（`Retryable`），临时限流 429（`Transient`）可重试但不计数；额度耗尽 429 虽计入健康但绝不把同一服务商加入本请求重试队列（`selection::is_retryable_with_message`），只切换候选；其余 4xx（含 400/413/422）作为 `FailureClass::ReturnToClient` 回传调用方——正文是含 `error` 对象的合法 JSON 时按字节原样透传，否则保留上游状态并包装成标准错误信封（message 含上游状态与可读正文，绝不含本地/上游凭据或请求头），非流式与流式分支规则一致。网络错误、非 JSON 成功响应、无效 SSE、空流和输出前读取失败可重试；未被重定向处理的 3xx 沿用既有成功/响应格式规则。
- 网关自身生成的错误（请求解析失败、配置读取失败、未知路径、未授权、`/v1/models` 或其他转发路径上的方法错误、请求体非法、无候选）统一使用 `{"error":{"message","type","code","param":null}}` 信封（`error_envelope`；message、type、code 均非空，`param` 恒为 `null`），无候选取 `code: all_providers_unavailable`。
- `runtime_http::attempt_non_streaming` / `runtime_http::attempt_streaming` 采用 fallback-first：候选排序采用平滑加权轮询（Smooth Weighted Round Robin, SWRR，`selection::weighted_candidates`）：每个候选累加当前配置权重，选出当前动态权重最大者（平手按 provider.id 字典序决胜）并扣减总权重，剩余候选按其更新后的当前动态权重降序排列构成有序 fallback 序列；单候选/空候选直通；自动禁用或被禁用的服务商被前置过滤，不参与权重累加或调度。按本请求初轮候选顺序先完成不等待退避的初轮（会话亲和绑定服务商可用时排在首位，其余保持加权 fallback 序列；无会话或新会话首次请求直接按 SWRR 排序），成功立即返回；随后按各服务商最早冷却截止时间串行重试，同一截止时间保持初轮顺序，每请求最多一个上游进行中。每个可重试服务商首次请求加最多 5 次重试（合计最多 6 次）；仅当可服务候选数大于 1 时才把可重试失败加入重试队列，恰好一个可服务候选时初轮仅尝试一次即立即以标准错误终止——不排队、不应用 `Retry-After` 等待、不消耗 120 秒预算，非流式与流式一致。多候选耗尽后沿用 `all_providers_unavailable` 格式：非流式与任何在写出首字节前确定的流式失败都返回 502 + `application/json` 标准信封，流式不再返回 200 SSE 错误事件加 `[DONE]`。请求日志语义：无候选仍记 502；耗尽的非流式与流式终止行都记录最后一次观测到的上游状态（网络失败记 0），绝不改写为固定的传输层 502。
- 会话亲和（session affinity）：网关从固定的入站请求头优先级列表——`x-session-affinity`、`x-opencode-session`、`session-id`、`session_id`、`conversation_id`、`thread-id`、`x-session-id`——取第一个存在且 trim 后非空的头作为会话标识（入站头名已小写；空值或全空白视为不存在并继续向后查找，列表外的头绝不参与；`session-id` 先于 `thread-id`）。绑定键为会话值与 trim 后本地模型名的组合（`model` 缺失或全空白视为不存在）；与平滑加权轮询（SWRR）深度协同：无会话请求直接采用 SWRR 排序；某会话与模型尚无绑定时，在候选顺序解析的同一临界区内由 SWRR 决定首选上游并写入绑定，使跨账号会话流量严格按配置权重比例平滑分配；已有会话绑定时，仅把绑定服务商移到候选列表最前，其余候选保持更新后的 SWRR 降序 fallback 序列。绑定服务商不在本次可用候选中时立即改绑为实际完成请求的服务商（未命中计数归零、原绑定不产生任何尝试或失败计数）；绑定服务商仍可用而请求在其他服务商上到达终局结果时记一次未命中，连续第二次未命中迁移到完成该次请求的服务商；由绑定服务商完成的请求把未命中计数归零；下游取消不记录；无候选、无会话头或无有效模型既不读取也不写入绑定。绑定空闲超过 30 分钟视为不存在，表上限 1024 条并按最近最少使用逐出；绑定只存在于进程内存、绝不落盘、重启即无，且绝不放宽候选集——协议不一致、映射被禁用或自动禁用、服务商被禁用的服务商绝不因绑定被尝试；头名列表、其优先级、两次未命中阈值、空闲过期与条目上限均为常量，`api_gateway.json` 与 `api_gateway_usage.db` schema 不变。原因与取舍见 [Gateway Session Affinity Pins a Session and Model to One Upstream](.ai-workflow/notes/implemented/architecture/2026-09-20-gateway-session-affinity-routing.md) 与 [API Gateway Smooth Weighted Round Robin Routes Requests and Fallback Candidates](.ai-workflow/notes/implemented/architecture/2026-09-20-api-gateway-weighted-routing.md)。
- 重试延迟由 `selection::retry_header_delay` / `selection::default_retry_delay` 决定：只读每个响应头的第一个值，有效 `retry-after-ms` 优先，其次 `Retry-After` 的非负有限秒数或未来 HTTP 日期，无效高优先级值继续向下回退；零表示立即，负数、非有限值、非法或过期日期无效。默认第 n 次重试等待 `min(2000ms * 2^(n-1) * (1 + random[0,1] * 0.25), 30000ms)`，即 2 秒起倍增、0–25% 正抖动、30 秒封顶；有效头指定等待不加抖动、不受 30 秒限制。
- 每次暂时失败完成后设置该服务商在本请求内的单调时钟冷却截止时间；长冷却不阻塞更早就绪候选。单请求仅累计实际退避等待，预算 120 秒，下一等待超过余额即终止，恰好等于余额允许；上游网络耗时不计入此预算，仍保留 10 秒连接与 60 秒空闲读取超时。冷却和预算不持久化、不跨请求共享；配置 schema 与公开 API 不变。
- `runtime_http::RequestHealth` 按入站请求汇总各映射行（`MappingTarget`：服务商 id 加 trim 后本地/上游模型名）最终结果，在请求正常结束时各应用一次：A 行失败而 B 行成功仍给 A 记一次，同一行重试恢复则清零；仅 404 / 临时限流 429 不累计健康失败，额度耗尽 429 计一次，期间另有网络/408/5xx 等健康失败则记一次；401/403 即时禁用该映射行且结算不重复计数。同一映射键（trim 后 `(local_model, upstream_model)`）的行共享计数，连续 3 个失败请求自动禁用该行（`FAILURE_THRESHOLD = 3`）；由服务商 `default_model` 服务的尝试没有映射行目标、不产生任何健康结算。流式仅在上游正常读完后成功清零；写出首字节后上游读取失败时，先补全 SSE 事件边界（最后一个转发字节非换行时补一个换行），再追加一个独立 `data: {"error":{...}}` 分片（type `server_error`、code `upstream_stream_error`）并关闭，绝不发送 `[DONE]`、绝不重试或切换候选，随后记一次失败（状态 502）并保留已累积 usage；下游取消不额外计为上游故障。映射行 `enabled` 表示用户意图、`auto_disabled` 表示运行状态，二者独立持久化，手动重新启用只清理运行状态而不改 `enabled`；`GatewayStatus.auto_disabled_count` 统计配置中 `auto_disabled` 为真的映射行数。
- `runtime_http::handle_connection` 在转发期间检测下游连接关闭或读取错误，取消未完成上游请求、退避等待及后续尝试；下游写错误亦停止。开始向下游写出响应头/体后禁止切换或透明重放；取消支持普通完整 HTTP 客户端断开，不增加半关闭协议支持。
- 上述架构决策的由来见本地 ADR 历史（`.ai-workflow/adr/`）；ADR 已停止新增，不再由 `ai-workflow` 子命令维护或读取，不要扫描目录或维护独立索引。

## 数据与存储不变量

- local-first：运行时以本地镜像为主，再按配置同步到 `local / iCloud / Git`。
- 主密码用于加密敏感数据（`secrets.rs` / `crypto.rs`），密钥不得写入日志或提交到仓库。
- 设置页按分区独立保存与重置；每个分区应保持幂等。
- 会话历史、用量与 CLI 配置文件由后端解析后回填前端，前端不直接读取这些文件。

## 代码约定

- 只做必要改动，匹配现有风格；不引入未使用的抽象或依赖。
- 新增库前先确认 `package.json` / `Cargo.toml` 已存在该依赖。
- 前端行为测试使用 vitest + @testing-library，测试文件与被测文件同目录；后端测试位于对应域的 `tests.rs` 或 `tests/` 子目录。
- 保持 `navigation.json` 与 `navigation.md` 同步，并确保 `ai-workflow context validate` 通过。

## 工作流约束

- Planning 逐条澄清业务影响问题并冻结 `spec.md` / `plan.md`；Plan-to-tasks 生成不可变任务文件。
- Coding 以 TDD 在项目内临时 worktree 实现单个已批准任务，完成后依次通过 Spec Review 与 Standards Review 才可合并。
- 架构、归属、公共符号、路径或工作流规则变化时，必须同步更新 `MEMORY.md` 与 `navigation.json` 并重新生成、校验 `navigation.md`。
- 架构、模块边界与归属、公共协议或 schema、跨领域标准、工作流或 agent 规则、难以回退的技术选型发生变更时，不再新增 ADR；决策原因与生命周期记录到 Agent Notes（`.ai-workflow/notes/`，以 `.ai-workflow/notes/README.md` 为格式与治理唯一来源）。每条 Note 均为英文正文加中文正文加 `.i18n.yaml` 一致性记录的三件套等权同体；仅自然语言正文做翻译，结构元素（`# Agent Note:` 前缀、标题、表格表头、`Status` 及其取值、字段名、路径、日期）保持英文，配对经 `ai-workflow notes pairing --write` 记录；`output_language` 只约束规划产物与会话表述，不约束 Note 语言。本文件记录当前标准（怎么做），与 Notes 不一致即为缺陷，须在同一变更内一起更新。
