# Archived notes instructions

Read `../README.md` for the authoritative archive policy and sealing procedure. This directory holds frozen historical implemented records, not current guidance by default.

Do not modify, move, delete or reorder a sealed English/Chinese/consistency-record triplet, or repair its outbound links. New archival work must preserve existing manifest entries and follow the README's review and append-only sealing procedure; sealing registers all three artifacts of each triplet. Never rebuild the manifest to accept changed history; report integrity failures.
